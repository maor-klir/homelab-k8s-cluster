#cloud-config

autoinstall:
  version: 1
  locale: en_US.UTF-8
  timezone: Europe/Berlin
  keyboard:
    layout: us    
  interactive-sections: []  # Explicitly forcing a non-interactive installation   
  identity:
    hostname: ubuntu-server
    username: ubuntu
    password: $6$rounds=50000$bMy1f1LiYrXcDCzd$0bu25U.UD79hDkmfw0iECmc5XmPaGeqEuKqjDhJgc7hKsmRMKqmlMqEvBDV2VLnL/3Bx2ofuZhqiyyqVqStkR/
  ssh:
    install-server: true
    allow-pw: true  # Allows password login
    ssh_quiet_keygen: true
    authorized-keys:
      - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIJhj7pZo8NRSMFGFkuUiO8hEJF+eohqfydweD/UUN2d/ maor@fedora-workstation
  network:
    network:
      version: 2
      ethernets:
        any:
          match:
            name: en*
          dhcp4: true
          dhcp6: false
  updates: security   # Default behavior.  Updates from the security pocket are installed.
  packages:
    - qemu-guest-agent
    - sudo
    - net-tools
    - perl
    - curl
    - vim
    - gnupg2
    - software-properties-common
    - apt-transport-https
    - ca-certificates
    - lsb-release
    - jq
    - cloud-init
  storage:
    layout:
      name: lvm
    swap:
      size: 0
  late-commands:
    - echo 'ubuntu ALL=(ALL) NOPASSWD:ALL' > /target/etc/sudoers.d/ubuntu
    - chmod 440 /target/etc/sudoers.d/ubuntu
    - |
      if [ -d /sys/firmware/efi ]; then
        apt install -y efibootmgr
        efibootmgr -o $(efibootmgr | perl -n -e '/Boot(.+)\* Ubuntu/ && print $1')
      fi